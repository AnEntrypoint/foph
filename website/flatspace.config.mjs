export default {
    contentDir: './content',
    outDir: './docs',
    theme: './theme.mjs',
    site: {
        title: 'Foph',
        tagline: 'JS rebuild of hermes-agent — pi-mono + flatspace + xstate + anentrypoint-design',
        baseUrl: '/',
    },
}
